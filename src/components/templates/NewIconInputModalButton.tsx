import { useState } from 'react';

const NewIconInputModalButton = () => {
  const [open, setOpen] = useState(false);
  const handleOpen = () => setOpen(true);
  const handleClose = () => setOpen(false);

  return <></>;
};

export default NewIconInputModalButton;
